# Gradle Settings
org.gradle.jvmargs=-Xmx2G
org.gradle.parallel=true

# Mod Properties (shared by every Minecraft version and loader)
mod_version=1.0.0
maven_group=fr.enimaloc
mod_package=fr.enimaloc.wyp
archives_base_name=whatyourpronouns

# Node-specific properties (minecraft_version, loader_version, fabric_version,
# architectury_version, neoforge_version, loom.platform, java_version, ...) live in
# versions/<version>-<loader>/gradle.properties, not here.
